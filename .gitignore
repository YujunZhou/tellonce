# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Install / runtime artifacts (用户装包后产生, 不进仓库)
install.log
*.tmp
*.bak

# Versioned 备份 (per `wf-pref-027` — 本地保留, 仓库不收)
*.pre_*.bak
*.v3_pre_pt_*.json
*.bak_*

# OS
.DS_Store
Thumbs.db

# 用户态运行数据 (个人项目-state, 不该入仓库)
# state/ 已默认走 <project>/.claude/preference-tracker-state/, 不在 skill 目录下
# 但若有人手动放, 防一下
state/
runtime/

# Install-time runtime artifacts (per-project state, obs_log, hooks).
# When install.sh runs inside repo root for testing, it writes here.
.claude/
.codex/
.copilot/preference-tracker-state/
.copilot/preference-tracker/
_review_diff.txt

# machine-specific python path recorded by install (run.ps1 launcher)
copilot/hooks/.python_path.txt
.python_path.txt

# preference-tracker runtime state (any depth)
**/.copilot/
**/preference-tracker-state/
**/__pycache__/
.pytest_cache/

# internal session/engineering notes — kept on disk for the author, not shipped public
SESSION_HANDOFF_v1.md
SESSION_HANDOFF_v2.md
AUDIT_REPORT_2026-06-05.md
MARKETING_HIGHLIGHTS.md
RELEASE_READINESS.md
PRE_PUBLISH_CHECKLIST.md
