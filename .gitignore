# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Install / runtime artifacts (produced after install, not committed)
install.log
*.tmp
*.bak

# Versioned backups (kept locally, not committed)
*.pre_*.bak
*.v3_pre_pt_*.json
*.bak_*

# OS
.DS_Store
Thumbs.db

# User-side runtime data (per-project state, not committed)
# state/ defaults to <project>/.claude/tellonce-state/, outside the skill dir
# guard against anyone placing it here manually
state/
runtime/

# Install-time runtime artifacts (per-project state, obs_log, hooks).
# When install.sh runs inside repo root for testing, it writes here.
.claude/
.codex/
.copilot/tellonce-state/
.copilot/tellonce/
_review_diff.txt

# machine-specific python path recorded by install (run.ps1 launcher)
copilot/hooks/.python_path.txt
.python_path.txt

# tellonce runtime state (any depth)
**/.copilot/
**/tellonce-state/
**/__pycache__/
.pytest_cache/

# internal session/engineering notes — kept on disk for the author, not shipped public
SESSION_HANDOFF_v1.md
SESSION_HANDOFF_v2.md
AUDIT_REPORT_2026-06-05.md
AUDIT_FINDINGS_2026-06-15.md
MARKETING_HIGHLIGHTS.md
RELEASE_READINESS.md
PRE_PUBLISH_CHECKLIST.md
I18N_AUDIT.md

# internal maintainer-only dev / port notes (kept local, not shipped public)
copilot/PORT_DESIGN.md
copilot/PORT_NOTES/
codex/docs/ROLLOUT.md
codex/docs/VERIFICATION.md

# personal fingerprint/seed overlays (kept local, never shipped)
copilot/lib/fingerprints.user.yaml
copilot/seed_memory.user/
**/fingerprints.user.yaml

# root (Claude Code) personal seed overlay (kept local, never shipped)
/seed_memory.user/

# tests - verified locally, kept out of the public tree (run locally: pytest lib/, codex/, etc.)
test_*.py
conftest.py
**/tests/
