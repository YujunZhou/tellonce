# seed_memory — intentionally empty

preference-tracker ships with NO pre-loaded memory rules. A new install starts
with an empty memory; the skill records YOUR preferences as you teach the agent.

If you want to ship starter rules with your own fork, drop `*.md` rule files
here and the installer will seed them into a fresh project's memory dir.
